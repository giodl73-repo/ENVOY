# ENVOY agent instructions

Read `CONTEXT.md`, then `.roles/ROLE.md`, before changing the repository.
Build visible assistance-realization features before reusable infrastructure.
Use public aggregates only; protect source, partner, and do-no-harm boundaries.
Run `cargo test --all-targets` and the README example before committing.
